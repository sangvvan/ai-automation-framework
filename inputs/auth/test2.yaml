id: test2
loginUrl: http://localhost:3000/auth/login
fields:
  username:
    locator:
      kind: role
      role: textbox
      name: Email
    value: ${SITE_USERNAME}
  password:
    locator:
      kind: role
      role: textbox
      name: Password
    value: ${SITE_PASSWORD}
  extras: []
submit:
  locator:
    kind: role
    role: button
    name: Sign in
postLogin:
  waitFor: []
expectsCaptcha: false
